general:
  branches:
    only:
      - dev
machine:
  environment:
    GCLOUD_PROJECT: "momentum-ai"

deployment:
    staging:
        branch: dev
        commands:
        - NODE_ENV=stage gulp deploy-frontend
